buildscript {
    ext.kotlin_version = '1.6.10'
    repositories {
        google()
        jcenter()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:4.1.0'
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
        classpath 'com.google.gms:google-services:4.4.2'
    }
}

// plugins {
//     id "com.android.application" version "7.3.0"
//     id "org.jetbrains.kotlin.android" version "1.7.10"
//     id("com.google.gms.google-services") version "4.4.2" apply false
//     // id "dev.flutter.flutter-plugin-loader" version "1.0.0" // This plugin is not needed
// }

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

// configurations {
//     all*.exclude group: 'org.jetbrains.kotlin', module: 'kotlin-stdlib-jdk7'
//     all*.exclude group: 'org.jetbrains.kotlin', module: 'kotlin-stdlib-jdk8'
// }

// rootProject.buildDir = "../build"
// subprojects {
//     project.buildDir = "${rootProject.buildDir}/${project.name}"
// }
// subprojects {
//     project.evaluationDependsOn(":app")
// }

// tasks.register("clean", Delete) {
//     delete rootProject.buildDir
// }

rootProject.buildDir = '../build'
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(':app')
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}

